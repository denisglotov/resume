This directory vendors the Liberation Sans TrueType fonts used by the resume
PDF builder. Keeping the font family in the repo makes local and CI builds use
the same font metrics.

The font license is included in `LICENSE.txt`.
